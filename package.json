{
  "name": "@teeviapp/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist",
    "publish": "npm publish -ws --access public",
    "pack": "npm pack -ws"
  },
  "devDependencies": {
    "tsup": "^8.3.6",
    "typescript": "^5.6.2"
  }
}
